# 절대 커밋 금지 — 세션 토큰/개인정보/캡처 원본
session_tokens.log
auth_headers.log
flows.mitm
flows*.mitm
*.er

# 로컬 설정(개인 userNum 등)
config.local
.env

# 다운로드한 게임데이터(님블뉴런 소유물 — 커밋 금지, 스크립트로 받을 것)
gamedb/
l10n-*.txt
gamedata*.zip

# 파이썬
__pycache__/
*.pyc
